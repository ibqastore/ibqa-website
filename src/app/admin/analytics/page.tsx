import styles from "../admin.module.css";
const metrics = [['Revenue','Rs. 0'],['Orders','0'],['Conversion','0.0%'],['Average order','Rs. 0']];
export default function AnalyticsPage() { return <div className="animate-fade-up"><div className={styles.pageHeader}><div><h1 className={styles.pageTitle}>Business analytics</h1><p style={{color:'var(--text-secondary)',marginTop:'.4rem'}}>A single view for sales, orders and campaign performance.</p></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(140px,1fr))',gap:'1rem',marginBottom:'2rem'}}>{metrics.map(([label,value])=><div key={label} className={styles.card} style={{margin:0}}><p style={{color:'var(--text-secondary)',fontSize:'.75rem'}}>{label}</p><strong style={{fontSize:'2rem',fontFamily:'var(--font-heading)'}}>{value}</strong></div>)}</div><div className={styles.card}><h2>Sales trend</h2><div style={{height:'230px',marginTop:'1.5rem',display:'grid',placeItems:'center',background:'#f7f3ed',color:'var(--text-secondary)'}}>Sales data will appear here once real orders are connected.</div></div></div>; }
